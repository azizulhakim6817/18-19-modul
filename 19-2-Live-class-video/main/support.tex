1. What is i?
//List By keyword Servivce.................
export const ListByKeywordService = async (req) => {
  try {
    let keyword = req.params.keyword;
    let regex = { $regex: keyword, $options: "i" };
    let SearchParams = [{ title: regex }, { shortDes: regex }];
    let SearchQuery = { $or: SearchParams };
    let MatchStage = { $match: SearchQuery };

2. What is upsert & data set/upsert: true?
//upsert data create ...............
    let postJsonData = {
      productID: productID,
      userID: user_id,
    };

    let data = await wishModel.updateOne(
      postJsonData,
      { $set: postJsonData },
      { upsert: true }
    );